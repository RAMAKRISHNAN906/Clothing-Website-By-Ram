import styled from "styled-components";

const SectionContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
`;

const SectionHeading = styled.h1`
  text-transform: capitalize;
`;

export { SectionContainer, SectionHeading };
